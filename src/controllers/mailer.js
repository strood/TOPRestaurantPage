"use strict";

// const nodemailer = require("nodemailer");

const sendEmail = (sender, name, body) => {
  console.log(sender);
  console.log(name);
  console.log(body);
}


export default sendEmail;
